[defaults]
inventory = inventory.ini
retry_files_enabled = False
verbosity = 1
nocows = 1

[privilege_escalation]
# Prompt once, before the play starts, instead of timing out when sudo asks
# for a password during fact gathering.
become_ask_pass = True
